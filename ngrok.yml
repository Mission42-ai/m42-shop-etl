version: "2"
authtoken: YOUR_NGROK_AUTH_TOKEN
tunnels:
  webhook:
    addr: 3000
    proto: http
    host_header: "localhost:3000"
  mcp:
    addr: 3001
    proto: http
    host_header: "localhost:3001"